# state

This folder defines the raw control state and the discrete feature mapping used
by the tabular RL controller.

- `features.py`: state initialization, clamping, EMA updates, and bucketed
  featurization.
