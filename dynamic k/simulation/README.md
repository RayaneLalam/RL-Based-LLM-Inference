# simulation

This folder contains the lightweight serving simulator used to train and
evaluate the controller before real-model runs.

- `env.py`: evolving workload/load model plus simulated speculative acceptance.
