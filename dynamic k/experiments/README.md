# experiments

This folder contains reproducible experiment runners.

- `run_benchmarks.py`: trains controllers, runs cached autoregressive and
  speculative policies, writes CSV and JSON outputs, and saves benchmark plots.
