# utils

This folder contains shared helpers.

- `metrics.py`: simulator and real latency-aware rewards plus state updates.
- `model_config.py`: environment-driven model profile resolution so model pairs
  can be changed per machine without code edits.
